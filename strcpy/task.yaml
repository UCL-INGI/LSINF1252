accessible: true
author: Nicolas Rybowski
context: On vous demande d'implémenter une variante de la fonction `strcpy <https://linux.die.net/man/3/strcpy>`_.
    Votre fonction prendra en argument le buffer source et retournera un pointeur
    vers la zone mémoire nouvellement allouée sur la heap.
environment: pyjavacpp
evaluate: best
groups: false
limits:
    time: '30'
    output: '2'
    memory: '100'
name: '[S3] Improved strncpy'
network_grading: false
order: 7
problems:
    strcpy_impl:
        type: code
        name: buf_strcpy
        header: |
            Write the body of the function *buf_strcpy*.

            .. code-block:: c

                /*
                * Create a buffer the same size as src, and copy the content of src to this buffer.
                *
                * @src: string to be copied
                * @return: return pointer. if src == NULL, return NULL
                */
                char *buf_strcpy(char *src) {
        language: c
stored_submissions: 0
submission_limit:
    amount: -1
    period: -1
tags: {}
weight: 1.0
