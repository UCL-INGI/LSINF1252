#include <stdlib.h>
#include <stdio.h>
#include "student_code.h"

int first(int* a){
    int *p = (int *)ptr;
    p = p+3;

    return *p;
}

char second(int* a){
    char *p = (char *)ptr;
    p = p+6;

    return *p;
}

int third(int* a){
    char *p = (char *)ptr;
    p = p+45;

    return *(int *)p;
}
