accessible: true
author: Mathieu Xhonneux
context: In this exercise, you will familiarize yourself with the functions ``printf(3)``
    (printing in the terminal) and ``sprintf(3)`` (text formatting).
environment: cpp
evaluate: best
groups: false
limits:
    memory: '100'
    output: '2'
    time: '30'
name: '[S2] Printing data'
network_grading: false
order: 7
problems:
    simple:
        type: code
        header: |-
            The function ``printf`` writes data to the standard output stream (*stdout*).

            Write the body of the function *print_foo* which will print "foo" (without the quotes) to *stdout*.

            .. code-block:: c

                void print_foo() {
        name: Foo
        language: c
    digit:
        language: ''
        header: Print a number (%d) + text
        type: code
        name: Digit
    example:
        name: ''
        header: ''
        type: match
        answer: ''
    unsigned-string:
        header: |-
            Print an unsigned char (%u) + string.

            Add QCM : what happens if we use %d with a char n > 127 ?
        name: Unsigned + string
        language: c
        type: code
    sprintf:
        type: code
        name: sprintf
        language: c
        header: |
            Exemple de sprintf :

            écrire dans buffer :

            sprintf(buffer, "Mister %s %c has %d eggs") -> Mister Olivier B. has 42 eggs

            .. code-block:: c

                char *format(char *buffer, unsigned int d, char *name, char initial) {
stored_submissions: 0
submission_limit:
    amount: -1
    period: -1
tags: {}
weight: 1.0
